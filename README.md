To-Do List App

This is a simple Python program that lets the user add tasks, view tasks, delete tasks, and quit the app. It uses basic Python concepts like lists, loops, functions, if/else, and simple error handling.

To run the program, open a terminal in the folder where the file is saved and type:

python toDoList.py
